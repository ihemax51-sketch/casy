<x-app-layout>
    <x-slot name="title">DDJ library</x-slot>
    <x-slot name="header">DDJ icon library</x-slot>
    <section class="page-heading"><div><span class="page-kicker">Resources / client assets</span><h1>All your item icons, indexed.</h1><p>Point CASY at the extracted media folder and it will scan DDJ files, validate them and attach them to catalog items.</p></div><div class="heading-actions"><form method="POST" action="{{ route('icons.scan') }}">@csrf<button class="button primary" type="submit" @disabled(!$profile)>Scan DDJ folder</button></form></div></section>
    @if(!$profile)<div class="empty-state large"><div class="empty-illustration blue" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M4 5h16v14H4zM8 9h8M8 13h5"/></svg></div><h2>Set an icon root first.</h2><p>Open server settings and add the folder where your extracted <code>.ddj</code> files live.</p><a class="button primary" href="{{ route('server-profile.index') }}">Configure icon path</a></div>@elseif(!$profile->icon_root)<div class="info-strip warning"><span class="info-icon" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M12 8v5m0 4h.01M10 3h4l7 7v4l-7 7h-4l-7-7v-4l7-7Z"/></svg></span><div><strong>No DDJ root configured</strong><p>Add a local or mounted game media path in Server settings, then run the scan.</p></div><a class="button secondary" href="{{ route('server-profile.index') }}">Server settings</a></div>@endif
    @if($profile)<section class="toolbar panel"><form method="GET" action="{{ route('icons.index') }}" class="search-form"><span class="search-symbol" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="m21 21-4.3-4.3m2.3-5.2a7.5 7.5 0 1 1-15 0Z"/></svg></span><input name="q" value="{{ $search }}" placeholder="Search icon filename or folder..."><button class="button secondary" type="submit">Search</button></form><div class="toolbar-meta"><span class="path-chip">{{ $profile->icon_root ?: 'Icon path not configured' }}</span><span class="muted-label">{{ number_format($icons->total()) }} indexed</span></div></section><section class="icon-grid">@forelse($icons as $icon)<article class="icon-card"><div class="icon-preview"><img src="{{ route('icons.thumbnail', $icon) }}" alt="{{ $icon->filename }}" loading="lazy"></div><div class="icon-card-copy"><strong>{{ $icon->filename }}</strong><code>{{ $icon->relative_path }}</code><span>{{ $icon->width && $icon->height ? $icon->width.' x '.$icon->height : 'DDJ asset' }} - {{ number_format(($icon->file_size ?? 0) / 1024, 1) }} KB</span></div></article>@empty<div class="empty-state"><div class="empty-illustration blue" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M4 5h16v14H4zM8 9h8M8 13h5"/></svg></div><h2>No DDJ assets indexed yet.</h2><p>Configure the root path and click Scan DDJ folder.</p></div>@endforelse</section>{{ $icons->links() }}@endif
</x-app-layout>
