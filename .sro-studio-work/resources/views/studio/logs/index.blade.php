<x-app-layout>
    <x-slot name="title">Audit log</x-slot><x-slot name="header">Audit & restore</x-slot>
    <section class="page-heading"><div><span class="page-kicker">Operations / accountability</span><h1>Every important action, traceable.</h1><p>CASY records who started an operation, what it touched, its risk level, and the result. Sensitive values are redacted before storage.</p></div><div class="heading-actions"><span class="security-note"><span class="security-dot"></span>{{ number_format($actions->total()) }} recorded actions</span></div></section>
    <section class="toolbar panel"><form method="GET" action="{{ route('logs.index') }}" class="search-form"><span class="search-symbol" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="m21 21-4.3-4.3m2.3-5.2a7.5 7.5 0 1 1-15 0 7.5 7.5 0 0 1 15 0Z"/></svg></span><input name="q" value="{{ $search }}" placeholder="Search action, operation or entity..."><select name="status"><option value="">All status</option>@foreach(['started','success','failed'] as $status)<option value="{{ $status }}" @selected(($filters['status'] ?? '') === $status)>{{ ucfirst($status) }}</option>@endforeach</select><select name="risk"><option value="">All risk</option>@foreach(['low','medium','high','critical'] as $risk)<option value="{{ $risk }}" @selected(($filters['risk'] ?? '') === $risk)>{{ ucfirst($risk) }}</option>@endforeach</select><button class="button secondary" type="submit">Filter</button><a class="button secondary" href="{{ request()->fullUrlWithQuery(['export' => 'csv']) }}">Export CSV</a></form><span class="muted-label">Newest first · {{ $actions->count() }} on this page</span></section>
    <section class="panel audit-panel"><div class="audit-table-wrap"><table class="audit-table"><thead><tr><th>Action</th><th>Entity</th><th>Risk</th><th>Mode</th><th>Status</th><th>When</th></tr></thead><tbody>@forelse($actions as $action)<tr><td><strong>{{ str($action->action)->replace('.', ' / ')->title() }}</strong><small>{{ $action->operation_id ? 'Operation '.$action->operation_id : 'Legacy action' }}</small>@if($action->summary)<details class="audit-details"><summary>Details</summary><pre>{{ json_encode($action->summary, JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES) }}</pre></details>@endif</td><td><code>{{ $action->entity_type ?: 'System' }}{{ $action->entity_key ? ' #'.$action->entity_key : '' }}</code></td><td><span class="risk-chip {{ $action->risk_level ?? 'low' }}">{{ strtoupper($action->risk_level ?? 'low') }}</span></td><td><span class="mode-chip">{{ $action->execution_mode ?: 'direct' }}</span></td><td><span class="audit-status {{ in_array($action->status, ['success','completed'], true) ? 'ok' : ($action->status === 'started' ? 'wait' : 'bad') }}">{{ $action->status }}</span></td><td class="audit-time">{{ $action->created_at->diffForHumans() }}</td></tr>@empty<tr><td colspan="6"><div class="empty-compact"><span aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M5 4h14v16H5zM8 8h8M8 12h6M8 16h4"/></svg></span><p>No audited actions yet.</p></div></td></tr>@endforelse</tbody></table></div></section>
    {{ $actions->links() }}
</x-app-layout>
