<x-app-layout>
    <x-slot name="title">Areas & regions</x-slot>
    <x-slot name="header">World & region studio</x-slot>

    <section class="page-heading area-page-heading">
        <div><span class="page-kicker">World content / instance worlds</span><h1>Clone an area safely</h1><p>Build an instance world from an existing template, keep terrain mappings intact, and review every dependency before execution.</p></div>
        <div class="heading-actions"><span class="security-note"><span class="security-dot"></span>Transaction protected</span></div>
    </section>

    @if(!$profile || $profile->connection_status !== 'connected')
        <div class="empty-state large"><div class="empty-illustration violet" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M3 20h18M5 20V8l7-5 7 5v12M9 20v-5h6v5M8 10h.01M12 10h.01M16 10h.01"/></svg></div><h2>Connect the shard database first.</h2><p>CASY needs a live shard connection before it can inspect worlds and terrain regions.</p><a class="button primary" href="{{ route('server-profile.index') }}">Open server settings</a></div>
    @elseif($error)
        <div class="inline-alert danger"><strong>Area studio could not be loaded</strong><span>{{ $error }}</span></div>
    @else
        <div class="area-workbench" x-data="{ regionIds: @js((string) old('region_ids', $form['region_ids'] ?? '')), worldId: @js((string) old('new_world_id', $form['new_world_id'] ?? '')), worldCode: @js((string) old('world_code', $form['world_code'] ?? '')), mode: @js((string) old('clone_mode', $form['clone_mode'] ?? 'quick')), regionCount() { return [...new Set(this.regionIds.split(/[\s,;]+/).filter(Boolean))].length } }">
            <main class="area-builder-column">
                <section class="panel area-template-panel"><div><span class="panel-kicker">Step 1 / source template</span><h2>Choose how to build the new area</h2><p>Select a world to copy its configuration and mapped regions, or start manually and paste Region IDs.</p></div><div class="template-controls"><label class="field"><span>Template world</span><select onchange="window.location=this.value ? '{{ route('regions.index') }}?source='+encodeURIComponent(this.value) : '{{ route('regions.index') }}'"><option value="">Manual / no template</option>@foreach($workspace['worlds'] as $world)<option value="{{ $world['ID'] }}" @selected((int)($workspace['world']['ID'] ?? 0) === (int)$world['ID'])>#{{ $world['ID'] }} / {{ $world['WorldCodeName128'] }} / {{ $world['region_count'] }} regions</option>@endforeach</select></label>@if($workspace['world'])<div class="template-source-meta"><span><b>{{ $workspace['world']['region_count'] }}</b> regions</span><span><b>{{ $workspace['world']['start_count'] }}</b> starts</span><span><b>#{{ $workspace['world']['ID'] }}</b> source</span></div>@else<div class="template-source-meta manual"><span><b>{{ number_format($workspace['total_region_count']) }}</b> terrain rows available</span></div>@endif</div></section>

                <form method="POST" action="{{ route('regions.preview') }}" class="panel area-clone-form">@csrf<input type="hidden" name="source_world_id" value="{{ old('source_world_id', $form['source_world_id'] ?? '') }}">
                    <div class="area-form-section"><div class="area-section-heading"><span class="area-step-number">2</span><div><span class="panel-kicker">New instance identity</span><h2>World details</h2></div><span class="status-chip success">IDs checked automatically</span></div><div class="field-grid two"><label class="field"><span>New world ID <b class="required-mark">*</b></span><input type="number" name="new_world_id" x-model="worldId" value="{{ old('new_world_id', $form['new_world_id'] ?? '') }}" min="1" max="2147483647" required><small>Next available ID is suggested and rechecked under a lock.</small></label><label class="field"><span>World CodeName <b class="required-mark">*</b></span><input name="world_code" x-model="worldCode" value="{{ old('world_code', $form['world_code'] ?? '') }}" maxlength="128" required><small>Must be unique in _RefGame_World.</small></label><label class="field"><span>Config group</span><input name="config_group" value="{{ old('config_group', $form['config_group'] ?? 'xxx') }}" maxlength="128" required><small>Copied from the template, or <code>xxx</code> for manual.</small></label><label class="field"><span>Max users</span><input type="number" name="max_users" value="{{ old('max_users', $form['max_users'] ?? 0) }}" min="0" max="32767" required><small>Maximum users inside this instance.</small></label></div><details class="advanced-world-settings"><summary>Advanced world behavior</summary><div class="field-grid four"><label class="field"><span>Type</span><input type="number" name="type" value="{{ old('type', $form['type'] ?? 0) }}" min="0" max="255" required></label><label class="field"><span>Max count</span><input type="number" name="max_count" value="{{ old('max_count', $form['max_count'] ?? 1) }}" min="0" max="32767" required></label><label class="field"><span>Entry type</span><input type="number" name="entry_type" value="{{ old('entry_type', $form['entry_type'] ?? 0) }}" min="0" max="255" required></label><label class="field"><span>Entrance type</span><input type="number" name="entrance_type" value="{{ old('entrance_type', $form['entrance_type'] ?? 0) }}" min="0" max="255" required></label><label class="field"><span>Leave type</span><input type="number" name="leave_type" value="{{ old('leave_type', $form['leave_type'] ?? 0) }}" min="0" max="255" required></label><label class="field"><span>Duration</span><input type="number" name="duration_time" value="{{ old('duration_time', $form['duration_time'] ?? 0) }}" min="0" max="2147483647" required></label><label class="field"><span>Empty remain</span><input type="number" name="empty_remain_time" value="{{ old('empty_remain_time', $form['empty_remain_time'] ?? 0) }}" min="0" max="2147483647" required></label></div></details></div>

                    <div class="area-form-section clone-mode-section"><div class="area-section-heading"><span class="area-step-number violet">3</span><div><span class="panel-kicker">Clone mode</span><h2>Choose the dependency scope</h2></div></div><div class="clone-mode-grid"><label class="clone-mode-card" :class="{ 'selected': mode === 'quick' }"><input type="radio" name="clone_mode" value="quick" x-model="mode"><span class="clone-mode-icon" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M13 2 4 14h6l-1 8 9-12h-6l1-8Z"/></svg></span><span><strong>Quick clone</strong><small>World settings, terrain mappings and optional start positions. Recommended for a new shell.</small></span></label><label class="clone-mode-card" :class="{ 'selected': mode === 'full' }"><input type="radio" name="clone_mode" value="full" x-model="mode"><span class="clone-mode-icon indigo" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M4 5h16v14H4zM8 9h8M8 13h5"/></svg></span><span><strong>Full clone</strong><small>Also remaps verified teleports and Hive/Nest spawn dependencies. It stops when the schema cannot prove a safe relationship.</small></span></label></div><div class="inline-alert warning" x-show="mode === 'full'" x-cloak><strong>Full clone is guarded.</strong><span>CASY validates every dependency inside the same transaction and rolls back on any unsupported relationship.</span></div></div>

                    <div class="area-form-section region-section"><div class="area-section-heading"><span class="area-step-number violet">4</span><div><span class="panel-kicker">Existing terrain mapping</span><h2>Regions to attach</h2></div></div>@if($workspace['source_regions'])<div class="source-region-preview">@foreach($workspace['source_regions'] as $region)<span><b>{{ $region['region_id'] }}</b><small>{{ $region['area_name'] ?: $region['continent_name'] ?: 'Terrain region' }}</small></span>@endforeach</div>@endif<label class="field area-region-input"><span>Region IDs <b class="required-mark">*</b></span><textarea name="region_ids" x-model="regionIds" rows="5" placeholder="25532, 25533, 25788, 25790" required></textarea><small>Every ID must exist in _RefRegion. One invalid ID cancels the entire transaction.</small></label><label class="start-position-option {{ !$workspace['world'] || !$workspace['start_positions'] ? 'disabled' : '' }}"><input type="checkbox" name="clone_start_positions" value="1" @checked(old('clone_start_positions', $form['clone_start_positions'] ?? false)) @disabled(!$workspace['world'] || !$workspace['start_positions'])><span><strong>Also copy template start positions</strong><small>{{ count($workspace['start_positions']) }} available. Disabled when the template has none.</small></span></label></div>
                    <div class="area-form-actions"><span><b x-text="regionCount()"></b> unique region(s) ready</span><button class="button secondary" type="submit">Preview SQL</button><button class="button primary" type="submit" formaction="{{ route('regions.store') }}" onclick="return confirm('Create this instance world in the live shard database?')">Create area</button></div>
                </form>
                @if($preview)<section class="panel sql-preview-panel"><div class="panel-heading"><div><span class="panel-kicker">Read-only preview</span><h2>Generated transaction</h2></div><span class="status-chip neutral">Not executed</span></div><p>The live action adds conflict checks, terrain validation, identity handling and rollback.</p><pre>{{ $preview }}</pre></section>@endif
            </main>

            <aside class="area-review-column"><section class="panel area-review-card"><span class="panel-kicker">Before you create</span><h2>Clone summary</h2><div class="review-identity"><span>New world</span><strong>#<i x-text="worldId || '—'"></i></strong><code x-text="worldCode || 'Enter a CodeName'"></code></div><div class="review-count"><strong x-text="regionCount()"></strong><span>terrain regions will be attached</span></div><ul><li><b>1</b><span>Check World ID and CodeName are free.</span></li><li><b>2</b><span>Verify every Region ID has terrain.</span></li><li><b>3</b><span>Review the selected dependency scope.</span></li><li><b>4</b><span>Create world and mappings in one transaction.</span></li></ul></section><section class="panel area-query-note"><span class="security-dot"></span><div><strong>Safe mapping behavior</strong><p>CASY creates _RefGame_World and mappings only. Terrain rows are reused, never duplicated. Full mode adds only verified dependencies.</p></div></section>
                @if($workspace['world'])<section class="panel area-existing-card"><div class="panel-heading"><div><span class="panel-kicker">Selected world #{{ $workspace['world']['ID'] }}</span><h2>Manage existing world</h2></div><span class="status-chip success">Live</span></div><form method="POST" action="{{ route('regions.update', $workspace['world']['ID']) }}" class="form-stack compact-form">@csrf @method('PUT')<div class="field-grid two"><label class="field"><span>World CodeName</span><input name="world_code" value="{{ $workspace['world']['WorldCodeName128'] }}" maxlength="128" required></label><label class="field"><span>Config group</span><input name="config_group" value="{{ $workspace['world']['ConfigGroupCodeName128'] }}" maxlength="128" required></label></div><div class="field-grid two"><label class="field"><span>Max users</span><input type="number" name="max_users" value="{{ $workspace['world']['WorldMaxUserCount'] }}" min="0" max="32767" required></label><label class="field"><span>Max count</span><input type="number" name="max_count" value="{{ $workspace['world']['WorldMaxCount'] }}" min="0" max="32767" required></label></div><details class="advanced-world-settings"><summary>Advanced fields</summary><div class="field-grid two"><label class="field"><span>Type</span><input type="number" name="type" value="{{ $workspace['world']['Type'] }}" min="0" max="255" required></label><label class="field"><span>Entry type</span><input type="number" name="entry_type" value="{{ $workspace['world']['WorldEntryType'] }}" min="0" max="255" required></label><label class="field"><span>Entrance type</span><input type="number" name="entrance_type" value="{{ $workspace['world']['WorldEntranceType'] }}" min="0" max="255" required></label><label class="field"><span>Leave type</span><input type="number" name="leave_type" value="{{ $workspace['world']['WorldLeaveType'] }}" min="0" max="255" required></label><label class="field"><span>Duration</span><input type="number" name="duration_time" value="{{ $workspace['world']['WorldDurationTime'] }}" min="0" max="2147483647" required></label><label class="field"><span>Empty remain</span><input type="number" name="empty_remain_time" value="{{ $workspace['world']['WorldEmptyRemainTime'] }}" min="0" max="2147483647" required></label></div></details><button class="button secondary full" type="submit">Save world settings</button></form><div class="existing-regions-heading"><span>Mapped terrain ({{ count($workspace['source_regions']) }})</span></div><div class="existing-region-list">@forelse($workspace['source_regions'] as $region)<div><span><b>{{ $region['region_id'] }}</b><small>{{ $region['area_name'] ?: $region['continent_name'] ?: 'Terrain region' }}</small></span><form method="POST" action="{{ route('regions.mappings.destroy', [$workspace['world']['ID'], $region['region_id']]) }}" onsubmit="return confirm('Remove this region mapping and its start positions?')">@csrf @method('DELETE')<button class="icon-button danger" title="Remove mapping" type="submit" aria-label="Remove mapping"><svg viewBox="0 0 24 24"><path d="M6 6l12 12M18 6 6 18"/></svg></button></form></div>@empty<span class="muted-label">No terrain mappings yet.</span>@endforelse</div><form method="POST" action="{{ route('regions.mappings.store', $workspace['world']['ID']) }}" class="mapping-add-form">@csrf<label class="field"><span>Add Region IDs</span><textarea name="region_ids" rows="2" placeholder="25532, 25533" required></textarea></label><button class="button tertiary full" type="submit">Add mappings</button></form></section>@endif
            </aside>
        </div>
    @endif
</x-app-layout>
